  //To print biggest number
  var num1 = prompt("Enter");
  var num2 = prompt("Enter");
  var num3 = prompt("Enter");

  if (num1 > num2) {
         if (num1 > num3) {
                document.write(`${num1} is biggest`);
         } else {
                document.write(`${num3} is biggest`);
         }
  } else {
         if (num2 > num3) {
                document.write(`${num2} is biggest`);
         } else {
                document.write(`${num3} is biggest`);
         }
  }

  //To find  wheather a num divisible by 2 and 3

  // const num = prompt('Enter a Num')

  // if (num % 2 === 0 || num % 3 === 0) {
  //     alert('Value is true')
  // }
  // else{
  //     alert('Value is false')
  // }

  //String Properties and methods
  // const name = "vamshi"
  // console.log(name.length);
  // console.log(name.toUpperCase());
  // console.log(name.toLowerCase());
  // console.log(name.slice(1))

  // let newname = name.replace("Marepu")
  // console.log(name)

